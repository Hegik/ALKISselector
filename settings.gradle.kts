plugins {
    // Lädt bei Bedarf automatisch ein passendes JDK für die Toolchain herunter.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "alkisselector"
